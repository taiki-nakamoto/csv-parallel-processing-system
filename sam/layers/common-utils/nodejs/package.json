{
  "name": "csv-processing-common-utils",
  "version": "1.0.0",
  "description": "Common utilities for CSV processing system (DateUtils, StringUtils, ValidationUtils, FormatConverters)",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}